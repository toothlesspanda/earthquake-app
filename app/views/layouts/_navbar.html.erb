<nav class="navbar navbar-expand-lg bg-primary-subtle" data-controller="navbar">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">
      <i class="fa-solid fa-earthquake"></i>
      Earthquake App
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarEarthquake" aria-controls="navbarEarthquake" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarEarthquake">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= root_path %>">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= earthquakes_path %>">Earthquakes</a>
        </li>
      </ul>

      <div>
        <% if current_user %>
          <div class="d-flex align-items-center justify-content-center">
            <span>
              <%= current_user.email %>
            </span>
            <%= button_to(destroy_user_session_path, method: :delete, class: "btn btn-link text-decoration-none")  do %>
              <i class="bi bi-box-arrow-right"></i>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>